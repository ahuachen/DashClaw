---
phase: 03-integration-guides
plan: 04
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03]
files_modified:
  - app/connect/page.js
  - app/self-host/page.js
  - README.md
autonomous: true
requirements: [GUIDE-06]

must_haves:
  truths:
    - "/connect page has a 'Framework Guides' section with 4 cards linking to /guides/claude-code, /guides/openai-agents-sdk, /guides/langgraph, /guides/crewai"
    - "/self-host page has guide links to all 4 framework guide pages"
    - "README has a link to /connect in the deploy or connect section"
    - "All 4 guide URLs are reachable from at least 2 navigation surfaces"
  artifacts:
    - path: "app/connect/page.js"
      provides: "Framework Guides card section"
      contains: "/guides/"
    - path: "app/self-host/page.js"
      provides: "Guide links section"
      contains: "/guides/"
    - path: "README.md"
      provides: "/connect link"
      contains: "/connect"
  key_links:
    - from: "app/connect/page.js"
      to: "/guides/claude-code"
      via: "Link component"
      pattern: "href.*guides/claude-code"
    - from: "app/connect/page.js"
      to: "/guides/openai-agents-sdk"
      via: "Link component"
      pattern: "href.*guides/openai-agents-sdk"
    - from: "app/connect/page.js"
      to: "/guides/langgraph"
      via: "Link component"
      pattern: "href.*guides/langgraph"
    - from: "app/connect/page.js"
      to: "/guides/crewai"
      via: "Link component"
      pattern: "href.*guides/crewai"
---

<objective>
Wire guide navigation from /connect, /self-host, and README so all 4 framework guides are discoverable.

Purpose: Fulfills GUIDE-06 — developers can find the guides from multiple entry points. /connect is the primary hub (per D-10), /self-host gets links (per D-12), README links to /connect as the guide selector (per D-11).

Output: 3 existing files modified with guide navigation links.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-integration-guides/03-CONTEXT.md
@.planning/phases/03-integration-guides/03-01-SUMMARY.md
@.planning/phases/03-integration-guides/03-02-SUMMARY.md
@.planning/phases/03-integration-guides/03-03-SUMMARY.md

<interfaces>
<!-- Current state of files to modify -->

From app/connect/page.js (current structure):
```javascript
import { headers } from 'next/headers';
import Link from 'next/link';
import { ChevronRight } from 'lucide-react';
import PublicNavbar from '../components/PublicNavbar';
import PublicFooter from '../components/PublicFooter';
import ConnectGuideClient from './ConnectGuideClient';
import { getConnectGuideContent } from '../lib/connectGuide.js';

export const dynamic = 'force-dynamic';

export default async function ConnectPage() {
  // ... renders PublicNavbar, breadcrumb, ConnectGuideClient, PublicFooter
}
```
The Framework Guides section must be added AFTER the ConnectGuideClient and BEFORE the PublicFooter.

From app/self-host/page.js (current structure):
```javascript
// Static component (no force-dynamic)
// Renders: hero, two-path intro, cloud path, what-you-deployed, skill section, local setup, verified agents
// Guide links should be added in a new section before PublicFooter
```

From README.md (current "Connect Your Agent" section at ~line 53):
```markdown
## Connect Your Agent
Three ways to get governed...
```
D-11 says README links to /connect (one link), not to individual guides.

Guide page routes created in plans 01-03 (all must be linked):
- /guides/claude-code (Plan 01 — GuideClient + hook-based governance)
- /guides/openai-agents-sdk (Plan 01 — Node.js SDK governance loop)
- /guides/langgraph (Plan 02 — Python SDK governance node)
- /guides/crewai (Plan 03 — Python SDK @tool decorator pattern)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Framework Guides card section to /connect page</name>
  <files>app/connect/page.js</files>
  <read_first>
    app/connect/page.js
    app/connect/ConnectGuideClient.js
    app/self-host/page.js
  </read_first>
  <action>
Modify `app/connect/page.js` to add a "Framework Guides" section between the ConnectGuideClient component and the closing PublicFooter. Per D-10, this is a 4-card section linking to the individual guide pages.

Add the following JSX inside the `<main>` block, after the `<ConnectGuideClient content={content} />` line and before the closing `</div>` of the max-w-5xl container:

```jsx
{/* Framework Guides - per D-10 */}
<section className="mt-12 rounded-3xl border border-[rgba(255,255,255,0.08)] bg-[#111] p-6 sm:p-8">
  <p className="text-xs uppercase tracking-[0.32em] text-zinc-500">Framework guides</p>
  <h2 className="mt-3 text-2xl font-bold tracking-tight text-white">Connect your framework</h2>
  <p className="mt-2 text-sm text-zinc-400">Step-by-step guides for popular agent frameworks. Each takes under 20 minutes.</p>

  <div className="mt-6 grid grid-cols-1 gap-4 sm:grid-cols-2">
    <Link href="/guides/claude-code" className="group rounded-2xl border border-[rgba(255,255,255,0.08)] bg-[#0d0d0d] p-5 transition-colors hover:border-brand/30">
      <h3 className="text-base font-semibold text-white group-hover:text-brand transition-colors">Claude Code</h3>
      <p className="mt-1 text-sm text-zinc-400">Govern Bash, Edit, Write, and MultiEdit tool calls via PreToolUse hooks. Zero SDK code required.</p>
    </Link>
    <Link href="/guides/openai-agents-sdk" className="group rounded-2xl border border-[rgba(255,255,255,0.08)] bg-[#0d0d0d] p-5 transition-colors hover:border-brand/30">
      <h3 className="text-base font-semibold text-white group-hover:text-brand transition-colors">OpenAI Agents SDK</h3>
      <p className="mt-1 text-sm text-zinc-400">Add guard-record-outcome governance to your OpenAI agent tools with the Node.js SDK.</p>
    </Link>
    <Link href="/guides/langgraph" className="group rounded-2xl border border-[rgba(255,255,255,0.08)] bg-[#0d0d0d] p-5 transition-colors hover:border-brand/30">
      <h3 className="text-base font-semibold text-white group-hover:text-brand transition-colors">LangGraph</h3>
      <p className="mt-1 text-sm text-zinc-400">Add a governance node to your LangGraph StateGraph with the Python SDK. Includes runnable example.</p>
    </Link>
    <Link href="/guides/crewai" className="group rounded-2xl border border-[rgba(255,255,255,0.08)] bg-[#0d0d0d] p-5 transition-colors hover:border-brand/30">
      <h3 className="text-base font-semibold text-white group-hover:text-brand transition-colors">CrewAI</h3>
      <p className="mt-1 text-sm text-zinc-400">Govern CrewAI tool calls using the @tool decorator pattern with the Python SDK. Includes runnable example.</p>
    </Link>
  </div>
</section>
```

This uses the same dark theme styling as the rest of /connect (bg-[#0d0d0d], border-[rgba(255,255,255,0.08)], text-zinc-400, hover:border-brand/30). The 4 cards are in a 2x2 grid on desktop, 1-column on mobile.

Do NOT modify ConnectGuideClient.js — the cards go in the server component (page.js) since they use Next.js Link and don't need client-side interactivity.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/connect/page.js contains `href="/guides/claude-code"`
    - app/connect/page.js contains `href="/guides/openai-agents-sdk"`
    - app/connect/page.js contains `href="/guides/langgraph"`
    - app/connect/page.js contains `href="/guides/crewai"`
    - app/connect/page.js contains `Framework guides` or `Framework Guides`
    - app/connect/page.js still contains `ConnectGuideClient` (existing content preserved)
    - app/connect/page.js still contains `PublicNavbar` (existing structure preserved)
    - app/connect/page.js still contains `PublicFooter` (existing structure preserved)
  </acceptance_criteria>
  <done>/connect page has a "Framework Guides" section with 4 cards linking to all guide pages, styled consistently with the existing dark theme.</done>
</task>

<task type="auto">
  <name>Task 2: Add guide links to /self-host and README</name>
  <files>app/self-host/page.js, README.md</files>
  <read_first>
    app/self-host/page.js
    README.md
  </read_first>
  <action>
**app/self-host/page.js** — Per D-12, add a guide links section. Add a new section before the `<PublicFooter />` at the end of the page. Insert it after the "verified agents" section (the last `<div className="max-w-5xl mx-auto mt-5 rounded-xl...">` block) and before `<PublicFooter />`.

Add this JSX:

```jsx
{/* Framework Integration Guides */}
<section className="pb-12 px-6">
  <div className="max-w-5xl mx-auto">
    <h2 className="text-2xl font-bold tracking-tight mb-2">Connect your agent framework</h2>
    <p className="text-zinc-400 mb-6">Step-by-step guides for popular agent frameworks. Each takes under 20 minutes.</p>
    <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-3">
      <Link href="/guides/claude-code" className="p-4 rounded-xl bg-[#111] border border-[rgba(255,255,255,0.06)] hover:border-brand/30 transition-colors">
        <h3 className="text-sm font-semibold text-white">Claude Code</h3>
        <p className="text-xs text-zinc-400 mt-1">Hook-based governance</p>
      </Link>
      <Link href="/guides/openai-agents-sdk" className="p-4 rounded-xl bg-[#111] border border-[rgba(255,255,255,0.06)] hover:border-brand/30 transition-colors">
        <h3 className="text-sm font-semibold text-white">OpenAI Agents SDK</h3>
        <p className="text-xs text-zinc-400 mt-1">Node.js SDK integration</p>
      </Link>
      <Link href="/guides/langgraph" className="p-4 rounded-xl bg-[#111] border border-[rgba(255,255,255,0.06)] hover:border-brand/30 transition-colors">
        <h3 className="text-sm font-semibold text-white">LangGraph</h3>
        <p className="text-xs text-zinc-400 mt-1">Python governance node</p>
      </Link>
      <Link href="/guides/crewai" className="p-4 rounded-xl bg-[#111] border border-[rgba(255,255,255,0.06)] hover:border-brand/30 transition-colors">
        <h3 className="text-sm font-semibold text-white">CrewAI</h3>
        <p className="text-xs text-zinc-400 mt-1">@tool decorator pattern</p>
      </Link>
    </div>
  </div>
</section>
```

Note: `app/self-host/page.js` already imports `Link` from `next/link`, so no new import needed.

**README.md** — Per D-11, README links to /connect (one link), not to individual guides. The README already has a "Connect Your Agent" section starting around line 53. Check if a link to /connect already exists in that section. If not, add a line at the end of the "Connect Your Agent" section (after the three options):

Add this line after the "Option 3: Use the SDK" section (around line 94, after the "4-step governance loop" line):

```markdown
For framework-specific step-by-step guides (Claude Code, OpenAI Agents SDK, LangGraph, CrewAI), visit [`/connect`](https://dashclaw.io/connect) on your DashClaw instance.
```

This is a single link per D-11 — /connect cards handle framework selection.
  </action>
  <verify>
    <automated>npm run lint 2>&1 | tail -5 && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - app/self-host/page.js contains `href="/guides/claude-code"`
    - app/self-host/page.js contains `href="/guides/openai-agents-sdk"`
    - app/self-host/page.js contains `href="/guides/langgraph"`
    - app/self-host/page.js contains `href="/guides/crewai"`
    - app/self-host/page.js still contains `PublicFooter` (footer preserved)
    - app/self-host/page.js still contains `SetupTabs` (existing content preserved)
    - README.md contains `/connect` somewhere in the "Connect Your Agent" section or nearby
    - README.md contains `framework` or `Framework` near the /connect link
    - `npm run build` succeeds with no broken Link href errors
  </acceptance_criteria>
  <done>/self-host page has 4 guide link cards. README has a single /connect link directing to framework guide selection. All 4 guides are navigable from /connect, /self-host, and README. Build succeeds confirming all Link hrefs resolve.</done>
</task>

</tasks>

<verification>
- `npm run lint` passes
- `npm run build` succeeds (catches broken Link hrefs)
- `npm run governance:boundary:check` passes
- /connect page contains links to all 4 guide pages
- /self-host page contains links to all 4 guide pages
- README contains /connect link
</verification>

<success_criteria>
- All 4 framework guides are reachable from /connect (4 cards per D-10)
- All 4 framework guides are reachable from /self-host (link cards per D-12)
- README links to /connect for framework guide selection (per D-11)
- GUIDE-06 is fully satisfied: all guides navigable from README, /connect, and /self-host
- `npm run build` passes confirming no broken routes
</success_criteria>

<output>
After completion, create `.planning/phases/03-integration-guides/03-04-SUMMARY.md`
</output>
