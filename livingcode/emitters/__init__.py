"""Emitters — generate derivative artifacts from the shape model.

An emitter takes the current ShapeModel and renders it as a target-specific
artifact (skill markdown, doctor check config, MCP tool definitions, etc.).
The purpose is so those artifacts never need to be hand-maintained.
"""
